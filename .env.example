# LLM Provider API Keys
# Configure your preferred provider in data/system/config.json

# Anthropic API Key (for Claude models)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (for GPT models)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# xAI API Key (for Grok models)
# Get your key from: https://console.x.ai/
XAI_API_KEY=your-xai-api-key-here

# Deployment Server (optional)
# Used by devops scripts: deploy-euno.sh, sync-data.sh, manage.sh
# Example: root@192.168.1.100 or user@example.com
EUNO_SERVER=

# Nextcloud Integration
# Create app passwords in Nextcloud: Settings > Security > Devices & Sessions
# Environment variable format: NEXTCLOUD_{INSTANCE_ID}_PASSWORD
# Instance IDs are configured in data/system/config.json under nextcloud.instances
NEXTCLOUD_PERSONAL_PASSWORD=your-app-password-here

# Google Calendar Plugin (optional)
# Create OAuth credentials at https://console.cloud.google.com/apis/credentials
# 1. Create a new project (or use existing)
# 2. Enable the Google Calendar API
# 3. Create OAuth 2.0 Client ID (Desktop application type)
# 4. Copy the client ID and secret below
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
