"""System tools - Config, dates, notifications, work control, consolidation, quotes.

Note: Modules are not eagerly imported here to avoid circular import issues.
Import specific modules directly when needed:
    from src.core.system.dates import parse_date
    from src.core.system.consolidation import Consolidation
"""
